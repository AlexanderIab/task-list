<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iablonski.crm.tasklist.repository.TaskRepository">
    <resultMap id="TaskResultMap" type="com.iablonski.crm.tasklist.domain.task.Task" autoMapping="false">
        <id property="id" column="task_id"/>
        <result property="title" column="task_title"/>
        <result property="description" column="task_description"/>
        <result property="status" column="task_status"/>
        <result property="expirationTime" column="task_expiration_date"/>
    </resultMap>

    <select id="findById" resultMap="TaskResultMap">
        SELECT t.id AS task_id,
        t.title AS task_title,
        t.description AS task_description,
        t.expiration_date AS task_expiration_date,
        t.status AS task_status
        FROM tasks.task_list t
        WHERE t.id = #{id}
    </select>

    <select id="findAllByUserId" resultMap="TaskResultMap">
        SELECT t.id AS task_id,
        t.title AS task_title,
        t.description AS task_description,
        t.expiration_date AS task_expiration_date,
        t.status AS task_status
        FROM tasks.task_list t
        JOIN tasks.user_task ut ON t.id = ut.task_id
        WHERE ut.user_id = #{id}
    </select>

    <insert id="assignToUserById">
        INSERT INTO tasks.user_task(task_id, user_id)
        VALUES (#{taskId}, #{userId})
    </insert>

    <insert id="create" keyProperty="id" keyColumn="id" useGeneratedKeys="true">
        INSERT INTO tasks.task_list(title, description, status, expiration_date)
        VALUES (#{title}, #{description}, #{status}, #{expiration_date})
    </insert>

    <update id="update">
        UPDATE tasks.task_list
        SET title = #{title},
        description = #{description},
        expiration_date = #{status},
        status = #{expiration_date}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE
        FROM tasks.task_list
        WHERE id = #{id}
    </delete>
</mapper>